/** zoom animation for collection images */
.collections-container .rimage__image {
  transition: transform 5s;
  &:hover {
    transform: scale(1.1);
  }
}

/** keep a 1:1 aspect ratio (square) */
.collections-container .rimage-wrapper {
  height: 0;
  padding-top: 100% !important;
}