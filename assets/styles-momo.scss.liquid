// homepage > categories (e.g. new, best sellers, on sale) > lower the labels to being close to the bottom of the images
.section--gallery .overlay .innest {
  margin-top: 50%;
}

.breadcrumbs {
  visibility: hidden;
}

.momo-hidden {
  display: none;
}

/** about page */
body#about {
  #shopify-section-page-template {
    background: url('https://cdn.shopify.com/s/files/1/3100/6776/files/about-back.jpg');
    background-size: cover;
    color: white;

    h1 {
      color: white;
    }
    
    p {
      background: rgba(0, 0, 0, 0.5);
      padding: 15px 20px;
    }
  }
}

/** homepage */
body#momosara {
  
  /** adjust "shop" button position on homepage */
  .slide-buttons {
    position: absolute;
    left: 50%;
    margin-top: 8.5em !important; // momo change - "shop button"
  }
/**
    This block affects all "Image with text" sections.
    Examples:
      - Shop by collections
      - See momo studio moments
  */
  .image-with-text-section {
    // margin-top: 0px;

    /** make it strech to full width */
    // .container {
    //   width: 100%;
    //   max-width: unset;
    //   margin-left: 0px;
    //   margin-right: 0px;

    // to make centered (todo: handle mobile!)
    //   .column.half {
    //     width: 50%;
    //     margin-left: 0;
    //   }
    // }

    /** text adjustments */
    .content {
      font-size: 17px;
      
      .h2-margin-top {
        margin-top: 1em;
      }
    }

  }

}